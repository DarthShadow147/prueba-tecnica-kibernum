version: '3.8'

services:
  api:
    build:
      context: ./PruebaTecnicaKibernum
    ports:
      - "5000:8080"
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=/data/app.db
    volumes:
      - appdata:/data

volumes:
  appdata: